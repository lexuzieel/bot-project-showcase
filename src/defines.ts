export enum CALLBACK_QUERY {
    START = "start",
    RESTART = "restart",
    NEXT = "next",
    BACK = "back",
    CANCEL = "cancel",
    DELETE = "delete",
    SKIP = "skip",
    CLEAR = "clear",
}
